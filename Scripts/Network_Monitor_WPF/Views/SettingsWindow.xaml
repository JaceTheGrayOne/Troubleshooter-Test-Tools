<Window
    x:Name="SettingsWindowRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
    Title="Network Monitor Settings"
    Width="800"
    Height="470"
    MinWidth="760"
    MinHeight="430"
    WindowStyle="None"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    Background="#080d10"
    UseLayoutRounding="True"
    SnapsToDevicePixels="True">
  <Window.Resources>
    <SolidColorBrush x:Key="WindowBrush" Color="#080d10"/>
    <SolidColorBrush x:Key="TitleBarBrush" Color="#101619"/>
    <SolidColorBrush x:Key="TitleBarHoverBrush" Color="#1b2429"/>
    <SolidColorBrush x:Key="TitleBarPressedBrush" Color="#263239"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="#0d1417"/>
    <SolidColorBrush x:Key="SurfaceAltBrush" Color="#151d22"/>
    <SolidColorBrush x:Key="GridLineBrush" Color="#333b42"/>
    <SolidColorBrush x:Key="BorderBrush" Color="#4b5258"/>
    <SolidColorBrush x:Key="TextBrush" Color="#f3f5f7"/>
    <SolidColorBrush x:Key="MutedBrush" Color="#aeb7bf"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#5db8ff"/>
    <SolidColorBrush x:Key="AccentDarkBrush" Color="#0078d4"/>
    <SolidColorBrush x:Key="RedBrush" Color="#ff4438"/>
    <SolidColorBrush x:Key="DisabledBrush" Color="#777d82"/>

    <Style x:Key="TitleButtonStyle" TargetType="{x:Type Button}">
      <Setter Property="Width" Value="38"/>
      <Setter Property="Height" Value="38"/>
      <Setter Property="Margin" Value="3,4,3,4"/>
      <Setter Property="Padding" Value="8"/>
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderBrush" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Focusable" Value="False"/>
      <Setter Property="shell:WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type Button}">
            <Border x:Name="ButtonChrome" Background="{TemplateBinding Background}" CornerRadius="0">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="ButtonChrome" Property="Background" Value="{StaticResource TitleBarHoverBrush}"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="ButtonChrome" Property="Background" Value="{StaticResource TitleBarPressedBrush}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="{x:Type TextBlock}">
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="FontFamily" Value="Segoe UI"/>
      <Setter Property="FontSize" Value="12"/>
    </Style>
    <Style TargetType="{x:Type TextBox}">
      <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource GridLineBrush}"/>
      <Setter Property="CaretBrush" Value="{StaticResource TextBrush}"/>
      <Setter Property="FontFamily" Value="Segoe UI"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Padding" Value="6,3"/>
      <Setter Property="Height" Value="26"/>
    </Style>
    <Style TargetType="{x:Type Button}">
      <Setter Property="Background" Value="{StaticResource SurfaceAltBrush}"/>
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource GridLineBrush}"/>
      <Setter Property="Padding" Value="10,5"/>
      <Setter Property="FontFamily" Value="Segoe UI"/>
      <Setter Property="FontSize" Value="12"/>
    </Style>
    <Style TargetType="{x:Type CheckBox}">
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="FontFamily" Value="Segoe UI"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>
    <Style TargetType="{x:Type ListBox}">
      <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource GridLineBrush}"/>
      <Setter Property="FontFamily" Value="Segoe UI"/>
      <Setter Property="FontSize" Value="12"/>
    </Style>
    <Style TargetType="{x:Type DataGrid}">
      <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource GridLineBrush}"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="HeadersVisibility" Value="Column"/>
      <Setter Property="GridLinesVisibility" Value="None"/>
      <Setter Property="RowHeaderWidth" Value="0"/>
      <Setter Property="CanUserAddRows" Value="False"/>
      <Setter Property="CanUserDeleteRows" Value="False"/>
      <Setter Property="AutoGenerateColumns" Value="False"/>
      <Setter Property="FontFamily" Value="Segoe UI"/>
      <Setter Property="FontSize" Value="12"/>
    </Style>
    <Style TargetType="{x:Type DataGridRow}">
      <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Style.Triggers>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Background" Value="{StaticResource TitleBarHoverBrush}"/>
          <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Trigger>
      </Style.Triggers>
    </Style>
    <Style TargetType="{x:Type DataGridCell}">
      <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource GridLineBrush}"/>
      <Setter Property="BorderThickness" Value="0,0,1,1"/>
      <Setter Property="Padding" Value="4,0"/>
      <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
      <Style.Triggers>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Background" Value="{StaticResource TitleBarHoverBrush}"/>
          <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Trigger>
      </Style.Triggers>
    </Style>
    <Style TargetType="{x:Type DataGridColumnHeader}">
      <Setter Property="Background" Value="{StaticResource SurfaceAltBrush}"/>
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource GridLineBrush}"/>
      <Setter Property="BorderThickness" Value="0,0,1,1"/>
      <Setter Property="Padding" Value="8,0"/>
      <Setter Property="Height" Value="28"/>
      <Setter Property="HorizontalContentAlignment" Value="Left"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>
    <Style TargetType="{x:Type TabControl}">
      <Setter Property="Background" Value="{StaticResource WindowBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource GridLineBrush}"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="0"/>
      <Setter Property="ItemsPanel">
        <Setter.Value>
          <ItemsPanelTemplate>
            <UniformGrid Rows="1"/>
          </ItemsPanelTemplate>
        </Setter.Value>
      </Setter>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type TabControl}">
            <Grid Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
              <Grid.RowDefinitions>
                <RowDefinition Height="28"/>
                <RowDefinition Height="*"/>
              </Grid.RowDefinitions>
              <ItemsPresenter Grid.Row="0"/>
              <Border
                Grid.Row="1"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="1"
                Background="{TemplateBinding Background}"
                SnapsToDevicePixels="True">
                <ContentPresenter
                  x:Name="PART_SelectedContentHost"
                  ContentSource="SelectedContent"
                  Margin="0"
                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
              </Border>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="{x:Type TabItem}">
      <Setter Property="Foreground" Value="{StaticResource MutedBrush}"/>
      <Setter Property="Background" Value="{StaticResource TitleBarBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource GridLineBrush}"/>
      <Setter Property="Height" Value="28"/>
      <Setter Property="FontFamily" Value="Segoe UI"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type TabItem}">
            <Border
              x:Name="TabChrome"
              Background="{TemplateBinding Background}"
              BorderBrush="{TemplateBinding BorderBrush}"
              BorderThickness="1,1,1,0"
              SnapsToDevicePixels="True">
              <ContentPresenter
                x:Name="ContentSite"
                ContentSource="Header"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                RecognizesAccessKey="True"
                TextElement.Foreground="{TemplateBinding Foreground}"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="TabChrome" Property="Background" Value="{StaticResource TitleBarHoverBrush}"/>
              </Trigger>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="TabChrome" Property="Background" Value="{StaticResource SurfaceBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{StaticResource DisabledBrush}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Style.Triggers>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
          <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        </Trigger>
      </Style.Triggers>
    </Style>
  </Window.Resources>

  <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Background="{StaticResource WindowBrush}">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="46"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="34"/>
      </Grid.RowDefinitions>

      <Grid x:Name="TitleBar" Grid.Row="0" Background="{StaticResource TitleBarBrush}">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock
          x:Name="TitleText"
          Grid.Column="0"
          Text="Network Monitor Settings"
          Foreground="{StaticResource TextBrush}"
          FontFamily="Segoe UI"
          FontSize="16"
          VerticalAlignment="Center"
          Margin="22,0,8,0"/>
        <Button x:Name="SettingsCloseButton" Grid.Column="1" Style="{StaticResource TitleButtonStyle}" ToolTip="Close">
          <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Button>
      </Grid>

      <TabControl x:Name="SettingsTabs" Grid.Row="1" Margin="12,12,12,0">
        <TabItem Header="Targets">
          <Grid Background="{StaticResource WindowBrush}" Margin="0">
            <DataGrid x:Name="TargetsGrid" Margin="14,14,186,56" SelectionMode="Single" SelectionUnit="FullRow"/>
            <StackPanel HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,14,30,0" Width="118">
              <Button x:Name="AddTargetButton" Content="Add" Height="30" Margin="0,0,0,8" Background="{StaticResource AccentDarkBrush}"/>
              <Button x:Name="DeleteTargetButton" Content="Delete" Height="30" Margin="0,0,0,18"/>
              <Button x:Name="MoveTargetUpButton" Content="Move Up" Height="30" Margin="0,0,0,8"/>
              <Button x:Name="MoveTargetDownButton" Content="Move Down" Height="30"/>
              <TextBlock Text="Color Preview" Foreground="{StaticResource MutedBrush}" Margin="0,28,0,6"/>
              <Border x:Name="TargetColorPreview" Height="36" BorderBrush="{StaticResource GridLineBrush}" BorderThickness="1" Background="{StaticResource AccentBrush}"/>
            </StackPanel>
          </Grid>
        </TabItem>
        <TabItem Header="Columns">
          <Grid Background="{StaticResource WindowBrush}" Margin="0">
            <TextBlock Text="Visible Columns" FontWeight="Bold" Margin="16,16,0,0"/>
            <ListBox x:Name="ColumnsVisibilityList" Width="220" Height="240" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="16,44,0,0"/>
            <TextBlock Text="Column Order" FontWeight="Bold" Margin="276,16,0,0"/>
            <ListBox x:Name="ColumnsOrderList" Width="220" Height="190" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="276,44,0,0"/>
            <Button x:Name="MoveColumnUpButton" Content="Move Up" Width="118" Height="30" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="512,44,0,0"/>
            <Button x:Name="MoveColumnDownButton" Content="Move Down" Width="118" Height="30" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="512,82,0,0"/>
            <TextBlock Text="Selected Width" Foreground="{StaticResource MutedBrush}" Margin="276,250,0,0"/>
            <TextBox x:Name="ColumnWidthTextBox" Width="98" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="398,247,0,0"/>
          </Grid>
        </TabItem>
        <TabItem Header="Timing">
          <Grid Background="{StaticResource WindowBrush}" Margin="0">
            <TextBlock Text="Refresh interval (ms)" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="22,26,0,0"/>
            <TextBox x:Name="RefreshIntervalTextBox" Width="110" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="308,22,0,0"/>
            <TextBlock Text="Ping timeout (ms)" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="22,66,0,0"/>
            <TextBox x:Name="PingTimeoutTextBox" Width="110" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="308,62,0,0"/>
            <TextBlock Text="History length" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="22,106,0,0"/>
            <TextBox x:Name="HistoryLengthTextBox" Width="110" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="308,102,0,0"/>
            <CheckBox x:Name="AutoStartCheckBox" Content="Auto-start monitoring on launch" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="22,154,0,0"/>
          </Grid>
        </TabItem>
        <TabItem Header="Health">
          <Grid Background="{StaticResource WindowBrush}" Margin="0">
            <TextBlock Text="Health" FontWeight="Bold" Margin="24,24,0,0"/>
            <TextBlock Text="DOWN after failed pings" Margin="24,65,0,0"/>
            <TextBox x:Name="DownFailuresTextBox" Width="96" Margin="246,62,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            <TextBlock Text="Orange after failed pings" Margin="24,105,0,0"/>
            <TextBox x:Name="OrangeFailuresTextBox" Width="96" Margin="246,102,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            <TextBlock Text="Orange rolling loss (%)" Margin="24,145,0,0"/>
            <TextBox x:Name="OrangeLossTextBox" Width="96" Margin="246,142,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"/>

            <TextBlock Text="RTT" FontWeight="Bold" Margin="392,24,0,0"/>
            <TextBlock Text="Green max (ms)" Margin="392,65,0,0"/>
            <TextBox x:Name="RttGreenTextBox" Width="96" Margin="584,62,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            <TextBlock Text="Yellow max (ms)" Margin="392,105,0,0"/>
            <TextBox x:Name="RttYellowTextBox" Width="96" Margin="584,102,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            <TextBlock Text="Orange max (ms)" Margin="392,145,0,0"/>
            <TextBox x:Name="RttOrangeTextBox" Width="96" Margin="584,142,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"/>

            <TextBlock Text="Loss" FontWeight="Bold" Margin="392,202,0,0"/>
            <TextBlock Text="Yellow max (%)" Margin="392,243,0,0"/>
            <TextBox x:Name="LossYellowTextBox" Width="96" Margin="584,240,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            <TextBlock Text="Orange max (%)" Margin="392,283,0,0"/>
            <TextBox x:Name="LossOrangeTextBox" Width="96" Margin="584,280,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"/>
          </Grid>
        </TabItem>
        <TabItem Header="General">
          <Grid Background="{StaticResource WindowBrush}" Margin="0">
            <CheckBox x:Name="AlwaysOnTopCheckBox" Content="Always on top" Margin="22,26,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            <CheckBox x:Name="DebugLoggingCheckBox" Content="Debug logging" Margin="22,64,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            <Button x:Name="ResetWindowPositionButton" Content="Reset Window Position" Width="172" Height="32" Margin="22,118,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            <Button x:Name="ResetDefaultsButton" Content="Reset to Defaults" Width="172" Height="32" Margin="22,168,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Background="{StaticResource AccentDarkBrush}"/>
          </Grid>
        </TabItem>
      </TabControl>

      <TextBlock
        x:Name="FeedbackText"
        Grid.Row="2"
        Text=""
        Foreground="{StaticResource MutedBrush}"
        Visibility="Collapsed"
        FontFamily="Segoe UI"
        FontSize="12"
        VerticalAlignment="Center"
        Margin="14,0,14,0"/>
    </Grid>
  </Border>
</Window>
