<Window
    x:Name="MainWindowRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
    Title="Network Monitor - Troubleshooter Test Tools"
    Width="1040"
    Height="270"
    MinWidth="820"
    MinHeight="260"
    WindowStyle="None"
    ResizeMode="CanResize"
    ShowInTaskbar="True"
    Background="#080d10"
    UseLayoutRounding="True"
    SnapsToDevicePixels="True">
  <Window.Resources>
    <SolidColorBrush x:Key="WindowBrush" Color="#080d10"/>
    <SolidColorBrush x:Key="TitleBarBrush" Color="#101619"/>
    <SolidColorBrush x:Key="TitleBarHoverBrush" Color="#1b2429"/>
    <SolidColorBrush x:Key="TitleBarPressedBrush" Color="#263239"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="#0d1417"/>
    <SolidColorBrush x:Key="SurfaceAltBrush" Color="#151d22"/>
    <SolidColorBrush x:Key="GridBrush" Color="#0b1215"/>
    <SolidColorBrush x:Key="GridAltBrush" Color="#0f171b"/>
    <SolidColorBrush x:Key="GridLineBrush" Color="#333b42"/>
    <SolidColorBrush x:Key="BorderBrush" Color="#4b5258"/>
    <SolidColorBrush x:Key="TextBrush" Color="#f3f5f7"/>
    <SolidColorBrush x:Key="MutedBrush" Color="#aeb7bf"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#5db8ff"/>
    <SolidColorBrush x:Key="AccentDarkBrush" Color="#0078d4"/>
    <SolidColorBrush x:Key="GreenBrush" Color="#61e243"/>
    <SolidColorBrush x:Key="YellowBrush" Color="#f5cf4f"/>
    <SolidColorBrush x:Key="OrangeBrush" Color="#ff9a3d"/>
    <SolidColorBrush x:Key="RedBrush" Color="#ff4438"/>
    <SolidColorBrush x:Key="DisabledBrush" Color="#777d82"/>

    <Style x:Key="TitleButtonStyle" TargetType="{x:Type Button}">
      <Setter Property="Width" Value="38"/>
      <Setter Property="Height" Value="38"/>
      <Setter Property="Margin" Value="3,4,3,4"/>
      <Setter Property="Padding" Value="8"/>
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderBrush" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Focusable" Value="False"/>
      <Setter Property="shell:WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type Button}">
            <Border x:Name="ButtonChrome" Background="{TemplateBinding Background}" CornerRadius="0">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="ButtonChrome" Property="Background" Value="{StaticResource TitleBarHoverBrush}"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="ButtonChrome" Property="Background" Value="{StaticResource TitleBarPressedBrush}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="{x:Type DataGrid}">
      <Setter Property="Background" Value="{StaticResource WindowBrush}"/>
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource GridLineBrush}"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="RowBackground" Value="{StaticResource GridBrush}"/>
      <Setter Property="AlternatingRowBackground" Value="{StaticResource GridAltBrush}"/>
      <Setter Property="HeadersVisibility" Value="Column"/>
      <Setter Property="GridLinesVisibility" Value="None"/>
      <Setter Property="RowHeaderWidth" Value="0"/>
      <Setter Property="RowHeight" Value="52"/>
      <Setter Property="ColumnHeaderHeight" Value="38"/>
      <Setter Property="CanUserAddRows" Value="False"/>
      <Setter Property="CanUserDeleteRows" Value="False"/>
      <Setter Property="CanUserResizeRows" Value="False"/>
      <Setter Property="CanUserReorderColumns" Value="True"/>
      <Setter Property="CanUserResizeColumns" Value="True"/>
      <Setter Property="AutoGenerateColumns" Value="False"/>
      <Setter Property="IsReadOnly" Value="True"/>
      <Setter Property="SelectionMode" Value="Single"/>
      <Setter Property="SelectionUnit" Value="CellOrRowHeader"/>
      <Setter Property="FontFamily" Value="Consolas"/>
      <Setter Property="FontSize" Value="14.666"/>
    </Style>

    <Style TargetType="{x:Type DataGridColumnHeader}">
      <Setter Property="Background" Value="{StaticResource SurfaceAltBrush}"/>
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource GridLineBrush}"/>
      <Setter Property="BorderThickness" Value="0,0,1,1"/>
      <Setter Property="Padding" Value="14,0,6,0"/>
      <Setter Property="HorizontalContentAlignment" Value="Left"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="FontFamily" Value="Segoe UI"/>
      <Setter Property="FontSize" Value="14.666"/>
      <Setter Property="FontWeight" Value="Bold"/>
    </Style>

    <Style TargetType="{x:Type DataGridCell}">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderBrush" Value="{StaticResource GridLineBrush}"/>
      <Setter Property="BorderThickness" Value="0,0,1,1"/>
      <Setter Property="Padding" Value="0"/>
      <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
      <Style.Triggers>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Background" Value="Transparent"/>
          <Setter Property="BorderBrush" Value="{StaticResource GridLineBrush}"/>
        </Trigger>
      </Style.Triggers>
    </Style>

    <DataTemplate x:Key="StatusCellTemplate">
      <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="18,0,6,0">
        <Ellipse Width="16" Height="16" Fill="{Binding StatusForeground}" VerticalAlignment="Center"/>
        <TextBlock
          Text="{Binding StatusText}"
          Foreground="{Binding StatusForeground}"
          FontFamily="Consolas"
          FontSize="14.666"
          FontWeight="Bold"
          Margin="12,0,0,0"
          VerticalAlignment="Center"/>
      </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="HistoryCellTemplate">
      <ItemsControl ItemsSource="{Binding HistorySamples}" Margin="12,0,12,0" VerticalAlignment="Center">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <UniformGrid Rows="1"/>
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Rectangle
              Width="5"
              Height="24"
              Fill="{Binding Brush}"
              HorizontalAlignment="Center"
              VerticalAlignment="Center"/>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </DataTemplate>
  </Window.Resources>

  <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Background="{StaticResource WindowBrush}">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="46"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <Grid x:Name="TitleBar" Grid.Row="0" Background="{StaticResource TitleBarBrush}">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock
          x:Name="TitleText"
          Grid.Column="0"
          Text="Network Monitor - Troubleshooter Test Tools"
          Foreground="{StaticResource TextBrush}"
          FontFamily="Segoe UI"
          FontSize="16"
          VerticalAlignment="Center"
          Margin="22,0,8,0"/>

        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
          <Button x:Name="SettingsButton" Style="{StaticResource TitleButtonStyle}" ToolTip="Settings">
            <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="18" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Button>
          <Button x:Name="RefreshButton" Style="{StaticResource TitleButtonStyle}" ToolTip="Reset monitor data">
            <TextBlock Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" FontSize="18" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Button>
          <Button x:Name="PinButton" Style="{StaticResource TitleButtonStyle}" ToolTip="Always on top">
            <TextBlock Text="&#xE718;" FontFamily="Segoe MDL2 Assets" FontSize="18" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" HorizontalAlignment="Center" VerticalAlignment="Center">
              <TextBlock.LayoutTransform>
                <RotateTransform Angle="-90"/>
              </TextBlock.LayoutTransform>
            </TextBlock>
          </Button>
          <Border Width="1" Height="30" Margin="8,8,8,8" Background="{StaticResource GridLineBrush}"/>
          <Button x:Name="MinimizeButton" Style="{StaticResource TitleButtonStyle}" ToolTip="Minimize">
            <TextBlock Text="&#xE921;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Button>
          <Button x:Name="MaximizeButton" Style="{StaticResource TitleButtonStyle}" ToolTip="Maximize or restore">
            <TextBlock x:Name="MaximizeGlyph" Text="&#xE922;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Button>
          <Button x:Name="CloseButton" Style="{StaticResource TitleButtonStyle}" ToolTip="Close">
            <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Button>
        </StackPanel>
      </Grid>

      <DataGrid
        x:Name="MonitorGrid"
        Grid.Row="1"
        Margin="0"
        HorizontalAlignment="Left"
        VerticalAlignment="Stretch"
        ScrollViewer.HorizontalScrollBarVisibility="Auto"
        ScrollViewer.VerticalScrollBarVisibility="Auto"/>
    </Grid>
  </Border>
</Window>
